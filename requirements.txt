# Python dependencies for the Research Assistant Starter Kit.
# Core needs very little. Install with:  python3 -m pip install -r requirements.txt

pyyaml>=6.0          # read research-config.yml in the setup renderer

# Paper build/extract engine (Markdown <-> Word)
python-docx>=1.1.0
markdown>=3.5

# Optional, for sqlite/duckdb database tiers:
# duckdb>=0.10
# pandas>=2.0
