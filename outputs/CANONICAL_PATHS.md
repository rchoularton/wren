# Canonical Paths

The single source-of-truth path for each shared dataset. Check here **before** loading a dataset, so you don't read a stale duplicate. Update this table whenever a dataset's canonical location changes.

| Dataset | Canonical path | Notes |
|---------|----------------|-------|
| _(example)_ | `outputs/<dir>/<file>` | _what it is, who uses it_ |

_Empty to start. Populate as your analysis produces shared datasets._
